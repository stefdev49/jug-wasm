{
  "name": "wasm",
  "version": "1.0.0",
  "description": "JUG WASM : code",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx http-server -a 0.0.0.0 -c-1"
  },
  "author": "stef",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.12.3"
  }
}
